{
  "name": "watch-plus",
  "version": "1.0.0",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "watch+": "src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build --compile src/index.ts --outfile watch+"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/diff": "^8.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "diff": "^8.0.3",
    "resend": "^6.9.2"
  }
}
